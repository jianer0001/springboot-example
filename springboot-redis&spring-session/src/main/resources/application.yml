

spring:
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    lettuce:
      pool:
        max-active: 8     #连接池最大连接数
        max-wait: -1ms    #连接池最大等待阻塞时间（负数表示没有限制）
        max-idle: 8       #连接池中最小空闲连接
        min-idle: 0       #连接池中最大空闲连接
server:
  port: 8081
